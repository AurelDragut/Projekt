<!-- Navigation -->
<nav class="navbar navbar-expand-lg navbar-light fixed-top bg-dark" id="mainNav">
    <div class="container">
        <a class="navbar-brand" href="/admin"><img src="/public/img/logo.png" height="60" alt=""></a>
        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav mr-auto">
                {% for menuitem in page.menu %}
                <li class="nav-item">
                    <a class="nav-link" href="/admin/{{ menuitem }}">{{ menuitem }}</a>
                </li>
                {% endfor %}
                <li class="nav-item">
                    {{ page.createIcon | raw }}

                </li>
            </ul>
        </div>
    </div>
</nav>
